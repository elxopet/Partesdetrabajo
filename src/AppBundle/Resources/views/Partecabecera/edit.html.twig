{% extends 'AppBundle::layout.html.twig' %}

{% block body -%}
    <h1>Partecabecera edit</h1>

    {{ form(edit_form) }}

        <ul class="record_Acciones">
    <li>
        <a href="{{ path('partecabecera') }}">
            Volver a la lista
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}

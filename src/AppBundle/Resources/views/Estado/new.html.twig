{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Estado creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('estado') }}">
            Volver a la lista
        </a>
    </li>
</ul>
{% endblock %}

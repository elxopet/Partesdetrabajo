{% extends 'AppBundle::layout.html.twig' %}

{% block body -%}
    <h1>Estado creation</h1>

    {{ form(form) }}

        <ul class="record_Acciones">
    <li>
        <a href="{{ path('estado') }}">
            Volver a la lista
        </a>
    </li>
</ul>
{% endblock %}

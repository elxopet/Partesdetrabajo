{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Partedetalle creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('partedetalle') }}">
            Volver a la lista
        </a>
    </li>
</ul>
{% endblock %}
